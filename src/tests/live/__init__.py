"""Live browser tests package.

These tests require a real Chrome instance with an authenticated Google account
and Gemini access. They are classified as ManualOnly and must NOT be collected
by the standard pytest run.

To run: pytest tests/live/ -v -s
"""

